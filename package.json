{
  "name": "multi-agent-simulation",
  "version": "0.0.1",
  "main": "server/src/app.js",
  "author": "Saladus",
  "license": "MIT",
  "scripts": {
    "start": "tsc -p server && tsc -p client && node server/out/app.js",
    "compile": "tsc -p server && tsc -p client",
    "compile-client": "tsc -p client"
  },
  "dependencies": {
    "@types/express": "^4.17.13",
    "express": "^4.17.1",
    "yarn": "^1.22.11"
  },
  "devDependencies": {
    "@types/node": "^16.9.1",
    "typescript": "^4.4.3"
  }
}
